# Config

Shared configuration for the monorepo, published to other workspaces as `@funtush/config`.

- `eslint-preset.mjs` â shared ESLint flat config, re-exported by the root `eslint.config.mjs`.

The repo is plain JavaScript (ESM), so there is no shared TypeScript config.
