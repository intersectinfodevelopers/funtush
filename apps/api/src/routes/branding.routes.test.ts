import { describe, it } from "vitest";

// Placeholder — branding route tests are not written yet. This keeps Vitest from
// failing the file with "No test suite found" (an empty *.test.ts errors).
describe.skip("branding.routes (TODO)", () => {
  it("has no tests yet", () => {});
});
